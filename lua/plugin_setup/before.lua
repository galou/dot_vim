-- Configuration for 'before.nvim'.
-- https://github.com/bloznelis/before.nvim

require('before').setup({
  -- How many edit locations to store in memory (default: 10)
  history_size = 20,
  -- Wrap around the end of the edit history (default: false)
  history_wrap_enabled = false,
})
